import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

  //nombre = 'Esteban'; // el valor de la variable se puede indicar en el html con interpolaciones, usando {{ nombre }}
}
